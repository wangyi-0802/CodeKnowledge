services:
  codeknowledge:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - chroma_data:/app/chroma_data
      - repo_cache:/app/repo_cache
      - ./.env:/app/.env:ro
    restart: unless-stopped

volumes:
  chroma_data:
  repo_cache: